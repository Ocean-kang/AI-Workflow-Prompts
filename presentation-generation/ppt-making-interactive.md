# PPT Making 交互执行模板

## 0. 使用场景

本模板适合在 ChatGPT 网页版中同时上传目标材料和 PPT 制作规划 Markdown 后使用，用于先检查材料是否齐全、制作要求是否明确，再生成可下载 `.pptx` 文件。

适合以下场景：

- 用户已经有 PPT 制作规划 Markdown，但不确定素材、模板、页数或输出方式是否足够明确。
- 用户希望 ChatGPT 先检查目标材料与 PPT 规划文件是否匹配，再开始制作。
- 用户希望最终仍复用 `ppt-making.md` 的直接制作逻辑，但在制作前增加 Ask-First 门控。

如果所有输入和制作要求都已经明确，可以直接使用 `presentation-generation/ppt-making.md`。

## 1. 交互式执行规则

你需要按两个阶段工作：

1. 阶段一：制作前检查与需求澄清；
2. 阶段二：正式 PPT 制作。

在用户明确说出以下任一类确认指令之前，你只能进行文件检查、需求澄清和默认假设确认，不能生成完整 PPTX、完整 `python-pptx` 脚本或完整 JSON slide spec：

- “开始制作”
- “直接生成”
- “开始生成 PPT”
- “按当前信息执行”
- “不用再问了”
- “可以开始”
- 其他语义等价的明确确认

如果用户只是上传文件、补充背景、回答问题或描述偏好，不代表已经允许你进入正式制作阶段。

每轮最多提出 5 个问题。只问会显著影响 PPT 文件生成、页面风格、素材使用、字体落实或事实准确性的问题。不要询问可以从上传文件或用户说明中直接判断的信息。

## 2. 阶段一：制作前检查与需求澄清

在澄清阶段，请先阅读用户上传的文件、任务说明和已有上下文，然后形成初步理解。

你必须按以下结构输出：

```md
## 我对制作任务的初步理解

## 文件与输入完整性检查

## 当前还需要确认的问题

## 默认假设

## 是否可以开始
```

文件与输入完整性检查必须优先确认：

- 是否已上传目标材料 / 原始依据；
- 是否已上传 PPT 制作规划 Markdown；
- 是否有 PPT 模板、图片、表格、Logo 或其他素材；
- 是否存在目标材料与规划文件明显冲突；
- 当前环境是否支持生成 `.pptx`。

如果信息已经基本足够，应提示：

> 当前信息已经基本足够。如果你认可上述理解和默认假设，可以回复“开始制作”，我将进入正式 PPT 制作阶段。

如果用户不回答部分问题，可以基于默认假设继续，但需要在正式制作前说明这些假设。

## 3. 本任务专属澄清问题

优先从以下问题中选择最关键的问题提问，每轮不超过 5 个：

1. 哪个文件是 PPT 制作规划 Markdown？
2. 是否必须直接生成可下载 `.pptx`，还是可以接受 `python-pptx` 脚本和 JSON slide spec 作为替代？
3. 是否有必须使用的 PPT 模板、学校/课题组风格、Logo 或配色？
4. 页面比例是否采用默认 16:9，还是需要 4:3 或其他比例？
5. 是否必须严格保持规划文件中的页数、页序和标题？
6. 是否允许对规划文件做必要微调，例如压缩过长页面、把细节移入备份页、把无数据图表改成概念图？
7. 是否需要 speaker notes / 讲者备注写入 PPT？
8. 如果规划文件与目标材料冲突，应优先修正为材料事实，还是优先保留用户指定表达并标注风险？
9. 是否接受默认字体：中文宋体，英文和数字 Times New Roman？
10. 输出文件名是否有固定要求？

如果用户没有明确回答，默认采用：

- 输出目标：优先生成可下载 `.pptx`；
- 替代方案：不能生成 PPTX 时输出完整 `python-pptx` 脚本、JSON slide spec 和人工制作说明；
- 页面比例：16:9；
- 字体：中文使用宋体，英文和数字使用 Times New Roman；
- 风格：简洁、专业、克制；
- 内容依据：目标材料优先于 PPT 制作规划 Markdown；
- 页数：尽量遵守 PPT 制作规划 Markdown，必要时将额外内容放入备份页；
- 备注：尽量生成 speaker notes；
- 微调：允许为事实准确性和页面可读性进行必要微调，并在制作报告中说明。

## 4. 阶段二：正式 PPT 制作

只有用户明确确认开始后，才进入正式制作阶段。

正式制作时，请复用 `ppt-making.md` 的任务逻辑。至少应覆盖：

1. 识别目标材料、PPT 制作规划 Markdown、模板/风格参考和素材文件。
2. 按输入优先级处理冲突：用户最新要求 > `ppt-making` Prompt > PPT 制作规划 Markdown > 目标材料 > 默认假设。
3. 核对 PPT 制作规划 Markdown 中的每页内容是否有目标材料依据。
4. 按规划文件制作 PPT 页面，包括标题、正文、图表、版式和 speaker notes。
5. 优先生成可下载 `.pptx` 文件。
6. 默认落实中文宋体、英文和数字 Times New Roman；无法完全落实时在制作报告中说明。
7. 如果不能直接生成 `.pptx`，输出完整 fallback：`python-pptx` 脚本、JSON slide spec、素材清单和人工制作说明。
8. 输出简洁制作报告，说明文件、页数、使用材料、制作规范落实情况、与规划文件的差异和仍需人工确认的信息。

正式制作阶段不要重新输出长篇需求澄清，也不要只输出摘要或规划。

## 5. 输出要求

需求澄清阶段的输出应简洁、可回答、可决策。不要输出完整 PPTX、完整脚本或完整 JSON slide spec。

正式制作成功时，请按以下结构输出：

```md
# PPT 制作完成

## 文件

## 使用材料

## 制作规范落实情况

## 与规划文件的差异

## 仍需人工确认的信息

## 备注
```

正式制作失败或环境不能生成 `.pptx` 时，请按以下结构输出：

```md
## 当前无法直接生成 PPTX 的原因

## 可采用的替代方案

## 完整 python-pptx 生成脚本

## JSON slide spec

## 素材清单

## 人工制作注意事项
```

## 6. 质量与事实约束

正式制作时必须遵守：

1. 不编造目标材料中没有的数据、结论、引用、实验结果、作者观点或项目进展。
2. PPT 制作规划 Markdown 只是制作蓝图，不是事实来源。
3. 规划文件与目标材料冲突时，默认以目标材料为准，并在制作报告中说明。
4. 缺失信息必须标注“材料中未明确说明”。
5. 每页只承担一个主要表达目标，避免大段文字堆叠。
6. 图表必须服务于论点，不添加纯装饰性视觉。
7. 字体默认要求是中文宋体，英文和数字 Times New Roman。
8. 如果环境不能写入 speaker notes、动画、模板母版或特定字体，需要在制作报告中说明。

## 7. 失败处理

如果上传文件无法读取、PPT 制作规划 Markdown 缺失、文件角色不清或任务目标冲突，应停留在需求澄清阶段，并说明：

- 哪些信息已经确认；
- 哪些信息缺失或冲突；
- 缺失信息会影响什么；
- 如果用户不补充信息，将采用什么默认假设。

如果用户要求跳过澄清并直接制作，可以进入正式制作阶段，但必须先列出关键默认假设。
